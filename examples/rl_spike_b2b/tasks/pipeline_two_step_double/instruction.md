Write a ZenML dynamic pipeline where a first step returns the integer 21 and a second step doubles the output of the first. The steps must be connected via their artifacts.

Save your pipeline as the file `/app/pipeline.py`. The file must run the pipeline when executed directly (use an `if __name__ == "__main__":` guard that calls the pipeline function).

You may execute your pipeline to check it works, read any traceback, and fix the file. You have at most 3 attempts. Only the final contents of `/app/pipeline.py` are graded.
