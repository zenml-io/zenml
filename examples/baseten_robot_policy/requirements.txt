zenml>=0.95.0
click
# torch is only needed wherever the training command actually runs:
#   - on Baseten it is already present in the public PyTorch base image;
#   - for a local smoke test (`python run.py --local`) install it yourself.
