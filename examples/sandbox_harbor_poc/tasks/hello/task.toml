schema_version = "1.2"
artifacts = []

[metadata]
description = "Smoke task: write '42' to /app/answer.txt so the verifier can read it back."

[verifier]
timeout_sec = 120.0

[verifier.env]

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 120.0
os = "linux"
allow_internet = true
mcp_servers = []
# No `docker_image` — the bridge ignores the local Dockerfile and
# falls through to the active Sandbox flavor's default image (Modal:
# python:3.11-slim). Set `docker_image = "..."` here only when a
# task needs to pin a specific registry image regardless of flavor.

[environment.env]

[solution.env]
