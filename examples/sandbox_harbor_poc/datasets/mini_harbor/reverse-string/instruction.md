The file `/app/input.txt` contains a single line of text. Reverse the characters and write the result to `/app/output.txt` (no trailing newline).

For example, if input.txt contains `hello`, output.txt should contain `olleh`.
