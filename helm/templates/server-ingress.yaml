{{- $server := include "zenml.serverValues" . | fromYaml -}}
{{- if $server.ingress.enabled -}}
{{- $fullName := include "zenml.fullname" . -}}
{{- $svcPort := $server.service.port -}}
{{- if and $server.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey $server.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set $server.ingress.annotations "kubernetes.io/ingress.class" $server.ingress.className}}
  {{- end }}
{{- end }}
{{- if and $server.ingress.tls.enabled (eq $server.ingress.className "nginx") }}
  {{- $_ := set $server.ingress.annotations "nginx.ingress.kubernetes.io/ssl-redirect" "true"}}
{{- end }}

{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "zenml.labels" . | nindent 4 }}
  {{- with $server.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and $server.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ $server.ingress.className }}
  {{- end }}
  {{- if $server.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ $server.ingress.host | quote }}
      secretName: {{ $server.ingress.tls.secretName }}
  {{- end }}
  rules:
    - http:
        paths:
          - path: {{ $server.ingress.path }}
            {{- if semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion }}
            pathType: Prefix
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
      {{- if $server.ingress.host }}
      host: {{ $server.ingress.host | quote }}
      {{- end }}
{{- end }}
