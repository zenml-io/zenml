# For Kubeflow compatibility, we add the service account to both
# the default and kubeflow namespace. This may be removed in a
# future version.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: zenml-service-account
  namespace: default
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: zenml-service-account
  namespace: zenml
