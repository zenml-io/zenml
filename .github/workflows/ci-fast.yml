---
name: ci-fast
on:
  workflow_dispatch:
  workflow_call:
  push:
    branches: [main]
    paths-ignore:
      - docs/**
      - '*'
      - '!pyproject.toml'
      - '**.md'
      - .github/workflows/claude.yml
      - .claude/**
  pull_request:
    types: [opened, synchronize, ready_for_review]
    paths-ignore:
      - docs/**
      - '*'
      - '!pyproject.toml'
      - '**.md'
      - .github/workflows/claude.yml
      - .claude/**
concurrency:
  # New commit on branch cancels running workflows of the same branch.
  # Use PR number when available for stable grouping across force-pushes;
  # fall back to ref for push/dispatch events.
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref
    }}
  cancel-in-progress: true
permissions:
  contents: read
  pull-requests: read
  issues: read
jobs:
  docstring-check:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: '3.10'
      - name: Install darglint using uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          source $HOME/.cargo/env
          uv pip install --system darglint
      - name: Check docstrings
        run: bash scripts/docstring.sh
  classify-changes:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false || github.event_name == 'workflow_dispatch'
    outputs:
      needs_migrations: ${{ steps.classify.outputs.needs_migrations }}
      needs_server_env: ${{ steps.classify.outputs.needs_server_env }}
      force_migrations: ${{ steps.classify.outputs.force_migrations }}
      force_server_env: ${{ steps.classify.outputs.force_server_env }}
      pilot_ci: ${{ steps.classify.outputs.pilot_ci }}
      default_test_scope: ${{ steps.classify.outputs.default_test_scope }}
    steps:
      - name: Classify changed files for selective jobs
        id: classify
        uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea  # v7.0.1
        with:
          script: |
            const failClosed = {
              needsMigrations: "true",
              needsServerEnv: "true",
              forceMigrations: "false",
              forceServerEnv: "false",
              pilotCi: "false",
              defaultTestScope: "integration",
            };
            const setOutputs = (outputs) => {
              core.setOutput("needs_migrations", outputs.needsMigrations);
              core.setOutput("needs_server_env", outputs.needsServerEnv);
              core.setOutput("force_migrations", outputs.forceMigrations);
              core.setOutput("force_server_env", outputs.forceServerEnv);
              core.setOutput("pilot_ci", outputs.pilotCi);
              core.setOutput("default_test_scope", outputs.defaultTestScope);
            };
            if (context.eventName !== "pull_request") {
              setOutputs(failClosed);
              return;
            }
            try {
              const prNumber = context.payload.pull_request.number;
              const mapPath = ".github/ci/selective-testing-map.json";
              const [changedFiles, labels] = await Promise.all([
                github.paginate(github.rest.pulls.listFiles, {
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  pull_number: prNumber,
                  per_page: 100,
                }),
                github.rest.issues.listLabelsOnIssue({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: prNumber,
                }),
              ]);
              let mapResponse;
              try {
                mapResponse = await github.rest.repos.getContent({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  path: mapPath,
                  ref: context.payload.pull_request.base.ref,
                });
              } catch (baseErr) {
                core.info(
                  `Map not found on base branch (${context.payload.pull_request.base.ref}), trying head branch.`
                );
                mapResponse = await github.rest.repos.getContent({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  path: mapPath,
                  ref: context.payload.pull_request.head.ref,
                });
              }
              if (Array.isArray(mapResponse.data) || !mapResponse.data.content) {
                throw new Error("Selective testing map could not be loaded as a file.");
              }
              const mapping = JSON.parse(
                Buffer.from(mapResponse.data.content, mapResponse.data.encoding).toString("utf8")
              );
              const ensureArray = (value, name) => {
                if (!Array.isArray(value) || value.length === 0) {
                  throw new Error(`Mapping key '${name}' must be a non-empty array.`);
                }
                return value;
              };
              if (
                typeof mapping.default_test_scope !== "string" ||
                typeof mapping.full_test_scope !== "string" ||
                !mapping.default_test_scope ||
                !mapping.full_test_scope
              ) {
                throw new Error(
                  "Mapping keys 'default_test_scope' and 'full_test_scope' must be non-empty strings."
                );
              }
              const allowedScopes = new Set([
                "integration",
                "integration/functional",
              ]);
              if (
                !allowedScopes.has(mapping.default_test_scope) ||
                !allowedScopes.has(mapping.full_test_scope)
              ) {
                throw new Error(
                  "Mapping scopes must be one of: integration, integration/functional."
                );
              }
              if (
                mapping.force_labels !== undefined &&
                (typeof mapping.force_labels !== "object" ||
                  mapping.force_labels === null ||
                  Array.isArray(mapping.force_labels))
              ) {
                throw new Error("Mapping key 'force_labels' must be an object when set.");
              }
              const toMatchers = (patterns) =>
                patterns.map((pattern) => new RegExp(pattern));
              const matchesAny = (matchers, path) =>
                matchers.some((matcher) => matcher.test(path));
              const migrationMatchers = toMatchers(
                ensureArray(mapping.migration_matchers, "migration_matchers")
              );
              const serverEnvMatchers = toMatchers(
                ensureArray(mapping.server_env_matchers, "server_env_matchers")
              );
              const fullScopeMatchers = toMatchers(
                ensureArray(mapping.full_scope_matchers, "full_scope_matchers")
              );
              const defaultForceLabels = {
                pilot_ci: "pilot-ci",
                migrations: "force-migrations",
                server_env: "force-server-env",
                full_scope: "force-full-integration",
              };
              const forceLabels = {
                ...defaultForceLabels,
                ...(mapping.force_labels || {}),
              };
              for (const [labelKey, labelName] of Object.entries(forceLabels)) {
                if (typeof labelName !== "string" || !labelName) {
                  throw new Error(
                    `Force label '${labelKey}' must map to a non-empty string.`
                  );
                }
              }
              const labelNames = labels.data.map((label) => label.name);
              const hasLabel = (labelName) =>
                Boolean(labelName) && labelNames.includes(labelName);
              const filePaths = changedFiles.map((file) => file.filename);
              const mappingFileChanged = filePaths.includes(mapPath);
              const needsMigrations =
                mappingFileChanged ||
                filePaths.some((path) => matchesAny(migrationMatchers, path));
              const needsServerEnv =
                mappingFileChanged ||
                filePaths.some((path) => matchesAny(serverEnvMatchers, path));
              const needsFullScope =
                mappingFileChanged ||
                filePaths.some((path) => matchesAny(fullScopeMatchers, path));
              const defaultTestScope =
                hasLabel(forceLabels.full_scope) || needsFullScope
                  ? mapping.full_test_scope
                  : mapping.default_test_scope;
              setOutputs({
                needsMigrations: needsMigrations ? "true" : "false",
                needsServerEnv: needsServerEnv ? "true" : "false",
                forceMigrations: hasLabel(forceLabels.migrations) ? "true" : "false",
                forceServerEnv: hasLabel(forceLabels.server_env) ? "true" : "false",
                pilotCi: hasLabel(forceLabels.pilot_ci) ? "true" : "false",
                defaultTestScope,
              });
            } catch (error) {
              core.warning(
                `Failed to classify changed files. Falling back to broad coverage. ${error}`
              );
              setOutputs(failClosed);
            }
  sqlite-db-migration-testing-random:
    needs: [classify-changes]
    runs-on: ubuntu-latest
    env:
      ZENML_ANALYTICS_OPT_IN: false
      ZENML_DEBUG: true
    # Fail closed: if classification fails, run migrations by default.
    if: always() && needs.classify-changes.outputs.pilot_ci != 'true' && (github.event.pull_request.draft
      == false || github.event_name == 'workflow_dispatch') && (needs.classify-changes.result
      != 'success' || needs.classify-changes.outputs.needs_migrations == 'true' ||
      needs.classify-changes.outputs.force_migrations == 'true')
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0
      - name: Set up Python 3.10
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: '3.10'
      - name: Test migrations across versions
        run: bash scripts/test-migrations.sh sqlite random
  spellcheck:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Spelling checker
        uses: crate-ci/typos@d01f29c66d1bf1a08730750f61d86c210b0d039d  # v1.27.0
        with:
          files: .
          config: ./.typos.toml
  api-docs-test:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Set up Python 3.11
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: '3.11'
      - name: Test API docs buildable
        run: bash scripts/generate-docs.sh -v DUMMY -c
  update-templates-to-examples:
    # this doesn't work on forked repositories (i.e. outside contributors)
    # so we disable template updates for those PRs / branches
    if: github.event.pull_request.head.repo.full_name == 'zenml-io/zenml' && github.event.pull_request.draft
      == false
    uses: ./.github/workflows/update-templates-to-examples.yml
    with:
      python-version: '3.11'
      os: ubuntu-latest
    secrets: inherit
  linting:
    if: github.event.pull_request.draft == false
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ['3.11']
      fail-fast: false
    uses: ./.github/workflows/linting.yml
    with:
      python-version: ${{ matrix.python-version }}
      os: ${{ matrix.os }}
    secrets: inherit
  ubuntu-setup-and-unit-test:
    needs: linting
    if: github.event.pull_request.draft == false
    strategy:
      matrix:
        # IMPORTANT: Since we are using the combination of `arc-runner-set`
        # and `3.10` in our `ci-fast` workflow, this combination has been
        # excluded from the `ci-slow` workflow. If you change the configuration
        # here, please adjust the configuration of `ci-slow` accordingly.
        os: [ubuntu-latest]
        python-version: ['3.11']
      fail-fast: false
    uses: ./.github/workflows/unit-test.yml
    with:
      python-version: ${{ matrix.python-version }}
      os: ${{ matrix.os }}
    secrets: inherit
  ubuntu-latest-integration-test-default:
    needs: [linting, classify-changes]
    if: always() && needs.linting.result == 'success' && needs.classify-changes.outputs.pilot_ci
      != 'true' && github.event.pull_request.draft == false
    uses: ./.github/workflows/integration-test-fast.yml
    with:
      os: ubuntu-latest
      python-version: '3.11'
      test_environment: default
      test_scope: ${{ needs.classify-changes.outputs.default_test_scope || 'integration' }}
      shards: '[1, 2]'
      max_parallel: 1
    secrets: inherit
  ubuntu-latest-integration-test-server:
    needs: [linting, classify-changes]
    # Fail closed: if classification fails, run server env by default.
    if: always() && needs.linting.result == 'success' && needs.classify-changes.outputs.pilot_ci
      != 'true' && github.event.pull_request.draft == false && (needs.classify-changes.result
      != 'success' || needs.classify-changes.outputs.needs_server_env == 'true' ||
      needs.classify-changes.outputs.force_server_env == 'true')
    uses: ./.github/workflows/integration-test-fast.yml
    with:
      os: ubuntu-latest
      python-version: '3.11'
      test_environment: docker-server-docker-orchestrator-mysql
      shards: '[1, 2]'
      max_parallel: 1
    secrets: inherit
