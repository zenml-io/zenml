deployments:
  - name: gcp-us-east4-server
    type: external
    setup: external
    config:
      url: "{{GCP_US_EAST4_SERVER_URL}}"
      username: "{{GCP_US_EAST4_SERVER_USERNAME}}"
      password: "{{GCP_US_EAST4_SERVER_PASSWORD}}"
    capabilities:
      server: true
