deployments:
  - name: gcp-europe-west-3-server
    type: server
    setup: external
    config:
      url: "{{GCP_EUROPE_WEST_3_SERVER_URL}}"
      username: "{{GCP_EUROPE_WEST_3_SERVER_USERNAME}}"
      password: "{{GCP_EUROPE_WEST_3_SERVER_PASSWORD}}"
    capabilities:
      server: true
