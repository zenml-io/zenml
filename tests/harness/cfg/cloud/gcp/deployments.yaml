deployments:
  - name: gcp-europe-west3-server
    type: server
    setup: external
    config:
      url: "{{GCP_EUROPE_WEST3_SERVER_URL}}"
      username: "{{GCP_EUROPE_WEST3_SERVER_USERNAME}}"
      password: "{{GCP_EUROPE_WEST3_SERVER_PASSWORD}}"
    capabilities:
      server: true
